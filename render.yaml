services:
  - type: web
    name: file-encryptor-web
    env: docker
    rootDir: website
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -Xmx512m -Xms256m
    healthCheckPath: /
